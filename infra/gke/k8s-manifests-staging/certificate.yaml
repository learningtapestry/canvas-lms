apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: canvas-staging-tls
  namespace: canvas-staging
spec:
  secretName: canvas-staging-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - canvas-staging.example.com
