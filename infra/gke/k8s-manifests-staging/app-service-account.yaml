apiVersion: v1
kind: ServiceAccount
metadata:
  name: canvas-app
  namespace: canvas-staging
  annotations:
    # Workload Identity → canvas-app GSA (Cloud SQL client, GCS object admin,
    # Secret Manager accessor).
    iam.gke.io/gcp-service-account: canvas-app@propane-country-501515-c2.iam.gserviceaccount.com
